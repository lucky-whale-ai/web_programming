{
  "name": "str-web-programming",
  "version": "1.0.0",
  "private": true,
  "description": "Учебный проект по веб-программированию",
  "scripts": {
    "api": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "0.17.4"
  }
}
